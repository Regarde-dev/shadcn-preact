{
  "name": "debounce",
  "type": "registry:lib",
  "description": "Debounce utility function",
  "files": [
    {
      "path": "share/debounce.ts",
      "type": "registry:lib",
      "content": "export function debounce(fn: (...args: any[]) => any, delay: number) {\n  let timeout: NodeJS.Timeout;\n  return (...args: any[]) => {\n    clearTimeout(timeout);\n    timeout = setTimeout(() => {\n      fn(...args);\n    }, delay);\n  };\n}\n"
    }
  ],
  "category": "lib"
}